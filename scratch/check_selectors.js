import { ethers } from "ethers";

console.log("currentPrice():", ethers.id("currentPrice()").slice(0, 10));
console.log("policies(address):", ethers.id("policies(address)").slice(0, 10));
console.log("contractBalance():", ethers.id("contractBalance()").slice(0, 10));
